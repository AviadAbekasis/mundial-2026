name: Update WC2026 Dashboard

on:
  push:
    branches: [main]
  schedule:
    # 05:00 UTC = 08:00 Israel time — the daily morning refresh: by 08:00 IL the
    # matchday has rolled to the upcoming 24h slate, freshly rebuilt.
    - cron: '0 5 * * *'
    # plus every 2 hours to keep scores / odds / analyses fresh through the day.
    - cron: '0 */2 * * *'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# allow only one concurrent deployment
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch, simulate, generate analyses, build dashboard
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python src/run.py 50000 --force

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: output

      - id: deploy
        uses: actions/deploy-pages@v4
